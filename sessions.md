# 学习会话日志（sessions）

> 起草：AI ｜ 内容确认：OZONGO
> 建档 2026-09-11 ｜ **契约 v2**（同日，4 字段 → 5 字段，新增 `category`）
> 本文件是 MP1（学习周报生成器）的**唯一输入数据源**。
> 铁律：本文件里**只有裸数据行会被解析**——其余每一行都必须以 `#` 或 `>` 开头，否则会被当成坏行。

## 输入契约 v2（解析器依此读取）

> 一行一条记录，**5 个字段**，用 `|` 分隔，顺序固定：`日期 | 周次 | 时长h | 类别 | 主题`
> 日期：`YYYY-MM-DD`（解析器暂不校验格式，见文末"已知缺口"）
> 周次：计划周序号，**规范字面** `W` + 无前导零的 ASCII 数字，范围 `W0`–`W28`（`W1` = 2026-09-07 ~ 09-13）；**不是 ISO 周号**。`W01`、全角 `W１`、`w1` 一律算坏行——分组只认字面，多一种写法就多裂一个桶
> 时长h：十进制小时，须为**有限非负**数（如 `2.5`、`0.8`）；`nan`／`inf`／负数一律算坏行
> 类别：**必须落在下方词表内**，词表外的值是坏行
> 主题：自由文本，**不得含 `|`**，永远放最后一列
> 行首、行尾**不加** `|`
> 以 `#` 或 `>` 开头的行是说明行，跳过；空行跳过
> 三段检查按此顺序判定：段数 ≠ 5 → 某段为空 → 值域不合（周次／类别／时长）

## 类别词表（受控值，与 `parse_sessions.py` 的 `CATEGORIES` 一字不差）

> `语法` —— 在学一个新语言知识点（含上课、实验、笔记成文）
> `项目` —— 在为某个交付物写或改代码（MP1–MP4、三练习、重构）
> `复习` —— 清到期卡、抽题、弱点卡
> `文档` —— 维护体系本身（看板、技能、CHANGELOG、仓库结构）
> `阅读` —— 读外部材料（Harness 讲义、TS 对照、课程）

> **边界裁决规则**：看这段时间**产出的是什么东西**，不看它算哪门学问——产出是一张笔记或一份实验记录 → `语法`；产出是可运行的代码变更 → `项目`；产出是 `queue/log` 里的评分变化 → `复习`；产出是文档或仓库结构的变化 → `文档`；只产出"读完了" → `阅读`。

> **为什么要加这个字段**：dict 分组只认哈希、不认语义。`主题` 是自由文本，两行字面不同的主题永远落不进同一个桶——按它统计"分布"得到的必然是每组一条。要出分布，就得有一个**受控**的维度；而"受控"不是写进契约就算数，是解析器会拒绝契约外的值。

## 计时与拆行规则

> **口径（2026-09-11 定）：本文件记的是「学习时间」——只有学习者本人动手或动脑的时段才计数。**
> 教练代做的机械劳动（跑命令、初始化仓库、同步 00/02/04/07 与 CHANGELOG、写契约与脚手架、代跑 git）**不计入**，也不另开一行——它不是学习产出，记进去只会把"本周学时"悄悄换成"在场时长"。
> 一行 ＝ 一个类别的一段。一场会话跨了类别，就按时间块拆成多行。
> 拆不出**可靠时间块**的（没有起止时间戳可依），整条归产出占大头的那一类——宁可粗，不可造。
> 每场收工由教练按实际起止时间追加一行，并当场删掉测试用行。
> **口径生效日 2026-09-11**：此日前入库的行按"单场在场时间"回填，**不回溯调整**（宁可粗，不可造）。其中 `2026-09-11 | W1 | 1.1 | 项目` 含教练代做的仓库与文档劳动，实际学习时长低于 1.1h——**已知偏高，留此标注，不改数**。

## 数据

2026-09-10 | W1 | 0.5 | 复习 | 开场清到期6卡与题面写法入规
2026-09-10 | W1 | 3.2 | 语法 | W3第6课raise与with文件读写与JSON
2026-09-11 | W1 | 0.3 | 复习 | 开场清到期5卡
2026-09-11 | W1 | 0.8 | 语法 | 挂账收口与MP1三段式讲课
2026-09-11 | W1 | 1.1 | 项目 | MP1解析段parse_sessions与契约升v2

## 为什么从 2026-09-10 起才有数据

> W0 复盘第 7 行当时写过"**W1 起逐日记录**"，实际没做到——看板 Done Log 只记"何时发生了什么"，从不记"持续多久"。MP1 正是来补这个洞的机制。
> 回填只做有硬证据的：`2026-09-10` 的 3.7h 出自看板 21:52 行原话"单场 18:11-21:52"，拆行分界取看板记的 18:38（6 卡清完）；`2026-09-11` 的分界取本场实际读到的本机时钟 10:22／10:41／11:31／12:33。
> 9/7–9/9 三天没有任何可靠的起止时间依据，**不回填估算值**：日志里放编出来的数字，比条目短更糟。你若记得那几天学了多久，自己加行。
> **已知缺口**：`date` 解析器目前一个字都不校验（`2026-13-99` 照收）。v1.0 加 `--week` 参数、要做真实日期比较之前必须先补这一关。
