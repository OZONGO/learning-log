# 学习会话日志（sessions）

> 起草：AI ｜ 内容确认：OZONGO
> 建档 2026-09-11。本文件是 MP1（学习周报生成器）的**唯一输入数据源**。
> 铁律：本文件里**只有裸数据行会被解析**——其余每一行都必须以 `#` 或 `>` 开头，否则会被当成坏行。

## 输入契约（解析器依此读取）

> 一行一条记录，**4 个字段**，用 `|` 分隔，顺序固定：`日期 | 周次 | 时长h | 主题`
> 日期：`YYYY-MM-DD`
> 周次：**计划周序号**，写法 `W0`–`W28`（`W1` = 2026-09-07 ~ 09-13）；**不是 ISO 周号**
> 时长h：十进制小时，能被 `float()` 解析（如 `2.5`、`0.8`）
> 主题：自由文本，**不得含 `|`**
> 行首、行尾**不加** `|`
> 以 `#` 或 `>` 开头的行是说明行，跳过；空行跳过
> 切出来字段数 ≠ 4 的行＝坏行，解析器**不得沉默**（跳过须计数，或直接抛错）

## 数据

2026-09-10 | W1 | 3.7 | raise与with、文件读写与JSON
2026-09-11 | W1 | 2.2 | MP1起步：三段式与数据契约、写解析器

## 为什么只有两条

> W0 复盘第 7 行当时写过"**W1 起逐日记录**"，实际没做到——看板 Done Log 只记"何时发生了什么"，从不记"持续多久"。MP1 正是来补这个洞的机制。
> 因此约定：本文件自 2026-09-11 起为正式记录，**每场收工由教练按实际起止时间追加一行**。
> `2026-09-10` 那条有硬证据（看板 21:52 行原话"单场 18:11-21:52"）；`2026-09-11` 那条按本场 10:22 起算、记到 12:33 为 2.2h（收工若延时再改）。
> 9/7-9/9 三天没有可靠的时长依据，**不回填估算值**：日志里放编出来的数字，比条目短更糟。你若记得那几天学了多久，自己加行。
