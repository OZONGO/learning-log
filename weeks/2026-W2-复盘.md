# 2026-W2 复盘（日期：9月14日 - 9月20日）

> 起草：AI（结构性内容与数据） ｜ 内容确认与正文：OZONGO
> 分工按 07 第六节：数据与索引归教练，复盘正文归本人。**本次例外**：卡壳三条本人授权教练依看板记录代记（9/20 指令），已逐条注出处与责任归属；红线自查 1、下周调整 4、给教练的话＝本人原话登记。

## 数据

- 计划学习：20h ／ 实际：**13.5h**（`sessions.md` 9/14-9/18 共 15 行：3.3 + 2.8 + 1.5 + 1.6 + 4.3；9/19 零记录；9/20 今日行收工补落）
- GitHub 提交：**44 次**（`learning-log` 18 ＋ `mini-projects` 5 ＋ `docs` 21；其中无意义提交：**0 次**，全部带类型前缀 `review:`／`w4:`／`mp1:`／`docs:`／`feat`／`test`／`fix`）
- 本周交付物：
  1. **MP1 v0.1 结版**（02 里它是 W3 交付，提前一周结掉）——41 测试用例三套齐、README 三节、架构与数据流复述过关（9/20 19:2x 本人终端 `py -3.13 -m unittest discover` → `Ran 41 tests OK`）
  2. W4 预习四块全落：`class` 与 `__init__`／继承与 `__str__`·`__repr__`·`@property`／`dataclass`／`pathlib`
  3. **02 文档 W4 练习②交出**：`python-100/w4/dir_report.py` 目录体检报告，六靶本人终端实测
  4. 两篇笔记成文入队：《断言 assert》（N013）＋《类与对象》（N014）
  5. `docs/` 目录首次进 git（此前"Done Log 只增不改"这条红线零技术守卫）
- 连续学习天数：**18 天在 9/19 归零**，9/20 重新起算（9/19 确认为休，未补记任何时段——`sessions.md` 不放没有依据的数字）

## 复习线（06 第六节）

- 本周到期卡：**17 卡次** ｜ 已清：**17** ｜ 未清：0 ｜ **执行率 100%**（按时清 15、逾期 2 张于 9/20 补清 → 按"当日清完"口径 88%）
- 逐日：9/14 1 张／9/15 5 张／9/16 4 张／9/17 4 张／9/18 1 张／9/19 0（未开工）／9/20 2 张
- 四档分布：**重来 0 ／ 困难 2 ／ 良好 15 ／ 简单 0**
- 两张困难都已闭合：WK010（9/15 缺"举例子"→9/17 良好结案）、N014（9/18 两处 P1 机制链缺口→9/20 全数销项且第二处超出补讲版）
- EF 均值走势：**2.48 → 2.48（走平）**。14 张活跃卡里 12 张 2.5、2 张 2.35（N010 9/13 困难、N014 9/18 困难）。走平的含义：本周没有新失手，但也没有一张卡被抬进"轻松"档——0 张"简单"
- 水蛭卡：本周新增 **0** ｜ 累计 **0**（全表失误次数为 0）
- 弱点变化：新增 **0** ｜ 结案 **2**（WK007 9/16、WK010 9/17）｜ 复现 **0**；开放弱点仅剩 **WK009**（9/23 到期）
- 面试遗忘点：不适用（AI 面试 W23 起，本周未开始）

## 红线自查（01 守则第六节）

1. **有没有提交过自己看不懂的代码？** **没有**（本人 9/20 答）。教练侧核对：本周四个 lab＋`dir_report.py` 全程走"预测注释先行→实测→对账"，每处交付当场抽查过；MP1 的三段职责与两份契约字面在结版关复述过关。
2. 能否不看代码口头复述当前项目架构与数据流？**能——9/20 结版关当场复述过，零卡点。** 三段各自职责／中间两份契约的**字面**形状（`rows` 五键含 `hours: float`、`problems` 三键、`report` 六键含两个 `list[tuple]` 及其排序方向）全部准确；`aggregate` 必须是纯库的四条理由（职责单一／可测试／可复用／避免重复入口）＋`render` 作唯一入口的三条外部动作（路径·串联·IO 只存在一份）。此条同时是本周最完整的产出之一。
3. 本周 AI 面试（如有）：**不适用**（W23 起每周日）。同类错题第二次出现：**是（1 例）**——N013 试金石标准句"如果把这条检查删掉，程序逻辑会变吗"9/16 与 9/20 两次点名均未原样产出，已在 `review/log.md` 记为升格自查项，下次按"给三个新场景当场判并说出这句"考。
4. commit 记录里有"存档／update"式提交吗？**没有**（44 笔全带类型前缀与具体内容）。

## 卡壳记录（本周最大的 3 个困难 ＋ 怎么解决的）

> 素材候选（教练按本周 Done Log 列出）：① N014 两处机制链缺口 ② "靶子会漂"（`demo_dir` 空目录靶被 `make_samples.py` 填实）③ 双解释器行为漂移 ④ 试金石标准句两次说不出 ⑤ `assert` 的"底层"只答到"是语句"。
> **本三条由教练代记**（本人 9/20 指示"你来完成，根据记录"），每条均注出处与责任归属，供本人核改。

**1. `inherit_lab.py` 的 property 量纲错配（9/16，P1）**
- 现象：`c.area = 5` 读回 `78.53975`——赋进去的值和读回来的值不是一回事；同时裸属性 `r` 可被 `c.r = -5` 绕开、负半径算出正面积。
- 根因：**混合，教练侧占一半**。看板 9/16 14:56 行教练自认在案——布置原话"是重算的，还是原来的"暗示了"setter 应该去改别的量"，等于把错误方向写进了题面。
- 解决：选方案 A（校验挂存储量 `r`、`area` 派生只读）。9/20 重测时本人自铸了课上没有的**对称反例**（若改存 `area` 派生 `r`，则 `area=-1` 已进对象、`r` 惰性计算或永不被访问即不报错），此处已彻底闭环。

**2. 验收靶子会漂——"空目录"靶被自己的造样本脚本填实（9/18）**
- 现象：交出"DoD 空目录不炸除零"那条证据时用的 `demo_dir/sub/sub2`，早已被 `make_samples.py` 写进过 `deep.txt`，**测的已不是那个靶**。补造 `probe_empty` 重跑（0 文件＋`exit=0`）才闭环。
- 根因：**流程设计，教练侧承担**。同族第三次现形（9/15 真数据恰好落在变异盲区、9/11 DoD 写 5 实测 6）。布置任务时没人规定"哪些靶会被后续步骤改状态"，"改后未测"就是必然发生的事，不是粗心。
- 解决：当场补靶重测；对策已写进"下周应调整第 2 条"（教练侧：DoD 里必须列明会被改状态的靶，且交证据须附靶子当前状态）。

**3. 机制链与固定表述：两处缺口 ＋ 一句话始终不出（9/18→9/20）**
- 现象：① `+=` 那一问只答了分水岭（list 有 `__iadd__`／str 走 `__add__`），"赋值最终落在哪一层"半问没答；② "校验为何必须挂存储量"整块空白；③ 试金石标准句"如果把这条检查删掉，程序逻辑会变吗"9/16、9/20 两次点名都未原样产出。
- 根因：**学习者侧为主，且与题面表述无关**——①②在 9/16 当课的复述关已交过（看板 9/16 14:56 行 Q4 记录在案），9/18 问不出＝**当场补讲、隔天蒸发**，这正是复习队列把 N014 钉死次日的理由；③属"判据在手上、话不在嘴上"，两次都是同一处。看板 9/18 13:44 行原话："**复习场内容蒸发最快**：10:55 刚补讲的机制、13:40 已问不出"。
- 解决：N014 当场评困难、EF 2.5→2.35、次日重测专打两处 → 9/20 两处全数销项（间隔 1→3 天）；试金石那句改判据为"给三个新场景，当场判并把这句说出来"，9/26 到期照打。

## 下周计划（W3：9.21-9.27 函数、模块与异常）

> 结构性抄写由教练完成，见 07 第二节；**调整项按本周弱点与"MP1 v0.1 提前结版"这一事实重排**。

- 知识点（02 W3）：`def`／参数／默认值／返回值；作用域；`*args`/`**kwargs`；`import` 与 `if __name__ == "__main__"`；`try/except/else/finally`；`raise`；`with` 与文件读写（文本/JSON）——**W3 整周知识点已于 9/7-9/10 与 9/15 全部预习完毕，正式周＝走流程＋产出**
- 练习①（02 原条目："把 W2 三个练习重构成函数库"）：**已取消**——重构线 2026-09-17 经用户裁决关闭（"不重构，以后也不重构"）
- 练习②：MP1 v0.1 启动 ＋ 交付 —— **已于 9/20 提前结版**，下周该项转为 **MP1 v1.0 起步**（`ReportGenerator` 类＋CLI 参数＋JSON 导出，属 W4 练习①）
- TS 对照 1h（W3 行）：函数类型、可选参数、泛型 `<T>` 入门
- Harness 课程点约 2.5h（04 映射 W3 行）：读 L02《Harness 到底是什么》→ MP1 子目录放入模板最小组合 → 用 Python 做一次弱／强 harness 对照
- 关 AI 自测三题（02 W3）：函数默认参数为什么不能用可变对象？`except Exception` 会吞掉什么？`with` 替你做了什么？
- 交付：本周提交 ≥5 次；MP1 v0.1 已结，v1.0 起步
- 复习线待办：9/21 到期 **3 张**（N001／N003／N009）；9/22 N011；9/23 **4 张**（N006／N012／WK009／N014）；9/26 2 张（N010／N013）；9/28 2 张（N007／N008）

## 下周应调整的 3 件事（教练提案，本人裁定）

1. **WK010 的结案标准有洞，要补一条"存量扫描"。** 今天结版前查出 6 处"陈述与实物不符"（四个文件头自称 v0.3、TD-1 开关注释挂着已废弃的 v0.2 标签、`parse_sessions.py` 写"23 个用例"而实际 24）。这些全部产生于 9/17 结案之前——**抽题良好测的是"知不知道这条规矩"，测不到"存量债务清没清完"**。建议：结案之外加一条习惯——**结版／结项这类节点动作前，先 `grep` 一遍陈述该模块的字面**（数字、版本、位置、文件名）。
2. **教练侧一条**：本周三次"靶子会漂"同族现形（9/15 真数据落盲区、9/11 DoD 写 5 实测 6、9/18 空目录靶被填实）。对策不在你身上——**布置动手任务时教练就要把"哪些靶会被后续步骤改状态"写进 DoD**，并要求交证据时附上靶子的当前状态（`ls`／文件数）。
3. **解释器这条要今天定死**（见下节裁决），否则"行为随版本变"的判断会继续污染验收。
4. **本人：无**（9/20 答"就按照现在的学习就好"）——本周节奏不加不减，第 1、2 条属"同一条流程换两侧执行"，不占新预算。

## 给AI教练的话

> **本人原话（9/20）**：「布置任务时必须清晰准确，我的所有卡壳记录都是因为你的表示不清。」

**教练接，并如实标一处不同意见**：
- **接下的部分**：确有记录在案。9/16 布置 `property` 那句"是重算的，还是原来的"暗示了错误方向（看板当场已自认一半）；9/18 把没讲过的 `suffix` 边界塞进摸底题＝考没教的东西（看板当场记教练一条）；9/10 题面里两个标签指向同一件事、traceback 的 File 行方向说反、崩点误判三起（`weaknesses.md` WK009 订正行）；9/11 把教练自己的误判当成学习者遗漏；今天布置"三条尾巴"时又漏扫两处（`render.py:2`、`aggregate.py:35`）。**"表述不清"在教练侧是真实存在的模式，不是偶发。** 落为可执行约定：**布置动手任务时，题面必须自包含**——不含暗示方向的问法、不含未讲过的判据、验收靶子的当前状态写死在 DoD 里；交作业时教练先自查"这句话是否可能有两种读法"，有就先补定义再让动手。
- **不同意见的部分**："**所有**卡壳都因教练表述不清"与记录不符，请本人复核第 3 条卡壳的归因。理由很实际：这份复盘 2027 年春招要当面试素材回读，把"当场补讲、隔天蒸发"（9/18 13:44 看板原话）和"判据在手上、话没在嘴上"（试金石两次未出）记成教练的锅，等于**把唯一需要修的那台机器写成不用修**——真丢了球的是谁，下周就得谁来补。若本人仍坚持原判，我照改，但在本行留下"教练持异"四个字，不留假账。

---

## 附：本周五天台账（教练按 Done Log 汇总，供你核）

| 日 | 学时 | 开场复习 | 主线 | 收工费曼 |
|---|---|---|---|---|
| 9/14 | 3.3h | 1 卡（N011 良好） | T16 自测三题 3/3＋T17 TS 对照＋MP1 渲染段全收尾 | 两道复述关代替 |
| 9/15 | 2.8h | 5 卡（良好4／困难1） | MP1 聚合段测试固化（三段测试齐）＋W4 第一块 `class`＋assert 课成文 | 判已交 |
| 9/16 | 1.5h | 4 卡（全良好，WK007 结案） | W4 第二块 继承／dunder／property（三处验收发现当场整改） | 一过一失 |
| 9/17 | 1.6h | 4 卡（全良好，WK010 结案） | W4 第三块 `dataclass`＋MP1 收尾两项（v0.1 结版裁定无尾巴） | 闸门倒置一次后补过 |
| 9/18 | 4.3h | 1 卡（N014 困难） | W4 第四块 `pathlib`＋W4 练习② `dir_report.py` 验收 | 4/4 且超课上（N014 两处失守） |
| 9/19 | 0 | — | 休 | — |
| 9/20 | 见收工 | 2 卡（全良好，N014 缺口销项） | W2 复盘＋MP1 v0.1 结版＋双解释器裁决 | 本场即复盘 |
