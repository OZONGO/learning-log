---
id: N006
title: 遍历工具与成绩单分析
created: 2026-09-05
topic: python-basics
weakness_ids: []
---

# W2 实战：遍历工具与成绩单分析（2026-09-05）

> 起草：AI ｜ 内容确认：OZONGO
> 分工：**零节清单与脚手架由 AI 起草**；一节＝AI 誊自当日会话素材（未加会话之外内容）；二节＝本人当日口答原话誊清；**三/四节待本人填写**。
> 关联：`../python-100/w2/score_analyzer.py`（练习②）、`../python-100/w2/word_freq2.py`（enumerate 改写）、`python-容器与词频统计.md`（sorted 尺子、元组解包）、`python-流程控制.md`（range 左闭右开）

---

## 零、待复述问题清单（AI 起草）

> **用法**：合上代码、合上本文，逐题作答。答不上来的那题，就是你以为会了其实没会的地方。
> 通关标准：10 题至少 8 题能**脱稿说清**，且第 7/8 题要能亲手推出三人并列时的名次序列。

1. `enumerate` 解决什么问题？它每一轮递给你的是什么？改写 `for i in range(len(words))` 套路后，循环体里还写 `words[i]` 为什么算"白学"？
2. 只需要元素、不需要序号时，最地道的写法是什么？
3. `zip` 干什么的？两个列表**长度不一致**时会发生什么？——必须说出**后果**，不止说条件（2026-09-05 费曼遗漏点）。
4. 直接 `print(zip(names, scores))` 会看到什么？想看内容怎么办？
5. `dict(zip(names, scores))` 利用了 `dict()` 能吃什么？
6. 遍历 `{学生: {科目: 分数}}` 两层循环，每一层"手里是什么类型"？
7. 同分并列你的规则原话是什么？用"分数比我高的人数"给"第几名"下个定义。
8. 代码里靠哪两个变量实现并列排名？`temp` 负责什么、`same` 负责什么？王五那一轮 `same` 为什么是 0？
9. 第一版 `rank -= 1` 在三人并列时为什么坏？现在这版 `rank - same - 1` 为什么对**任意长度**并列都成立？
10. 把手算预期写在代码注释里、跑完逐项对照——这是 L01 讲义里的哪个概念？孙七注释保留随时重开——这个习惯叫什么？

---

## 一、今日翻车与弯路（AI 誊自当日会话素材）

### 翻车①：预测 1,2,2,4,5，机器说 1,1,2,4,5
- **现象**：往成绩单加孙七造成三人并列（李四/赵六/孙七都是 91.0），预测旧代码（`rank -= 1` 版）会输出 1,2,2,4,5。
- **实际运行**：1,1,2,4,5——赵六打印的是 1，孙七才是那个错的 2。
- **我当初以为**："减 1"从第三个并列者才生效。
- **实际机制**：`rank -= 1` 对**每一个**撞上同分的都生效（赵六也被减，所以是 1）；真正漂移的是孙七（3-1=2，应该是 1）。
- **以后怎么避免**（本人原话）："脑测不一定准确，需要实际运行代码调试。"——预测→运行→对照，两者不一致时**先修脑内模型**；trace 要逐行跟着代码走，不凭"大概会这样"。

### 弯路②：same 计数器的差一错误
- 第一版修正 `rank = rank - same`：`same += 1` 写在减法**之后**，第一次撞车时 `same` 还是 0，减了个寂寞——赵六会被打成 2，把原本做对的两人并列也弄坏（脑内 trace 实验二亲手抓到）。
- 修正：`rank = rank - same - 1`。语义＝"自己的位置 − 前面并列人数 − 1 ＝ 并列组第一人的位置"，对任意长度并列成立。

---

## 二、我的理解（本人当日口答原话，未修饰）

> **zip 预测题（长度不齐会怎样）：**
> "张三 90、李四 85，不报错，因为默认以短的为准。"

> **"第几名"由什么决定：**
> "第几名由分数决定。"
> —— AI 补精确版：**分数比我高的人数 + 1**。本人后续原话："因为第一有 3 人，所以第四人及其分数相同的为第四名。"

> **same 的生命周期（王五那轮）：**
> "王五那一轮 same 是 0，因为张三与之前不同，same 清零；孙七打完，same 为 2，张三分数不同，清零，王五进 if。"

> **enumerate / zip 选型（收工费曼原话）：**
> "只遍历一个列表，但需要知道元素位置用 enumerate；遍历两个或多个列表，但不关心位置用 zip。"
> —— AI 补：还要一个判据——**序号本身要不要用**（打印"第 n 名"才值得动用 enumerate）。

> **并列规则与实现（收工费曼原话）：**
> "分数相同的为同一名次，下一个分数不同的人，的名次正常显示；用 temp 和 same。"
> —— AI 补精确化："正常显示"的本质是**占号跳号**：并列组占第 1，下一位直接第 4（因为 3 个人比它高）；temp 记上一轮分数（发现并列），same 记并列组里排在我前面的人数（把名次拉回组首）。

---

## 三、还能改进的地方（本人填写）

> 从这几个角度想，能想到别的更好：
> ① `Chinese_score` / `Math_score` / `English_score` 把科目名硬编码了 9 处——加一门"物理"要改几处？把那个难受劲儿写下来，它是 W3 函数课的引子（和 converter.py 四分支重复校验是同一味药）。
> ② `temp = 0` 当哨兵：现在安全是因为分数都是正数——万一有人平均分真是 0.0，第一轮会发生什么？哨兵的安全假设也要心里有数。
> ③ 手算注释（第 8-11 行）在改数据后要同步更新——"对照表过期"比没有对照表更危险，为什么？

（本人填写）

---

## 四、一句话总结（本人填写）

> 今天学到的、之前不知道的那**一件**事。**只能写一件。**
> 写不出来就说明今天没有一个清晰的收获——那就回去看第零节，看哪题答不上来。

（本人填写）
